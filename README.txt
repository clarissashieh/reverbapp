How to Install Reverb
1. Download the "reverbapp" folder onto your computer
2. Open Docker Desktop
3. Open Terminal, cd to the "reverbapp" folder in your files, and enter the following commands to run on Docker:
    chmod 755 *.bash
    ./docker-build.bash
    ./docker-run.bash
4. Enter the following command to run Reverb: python main.py